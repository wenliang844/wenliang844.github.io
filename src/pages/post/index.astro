---
import { buildStats } from "../../../scripts/build.mjs";
import { renderPostList } from "../../templates/post.mjs";
import { getPublishedPosts, markAstroOutput } from "../../lib/astro-posts";

const posts = await getPublishedPosts();
const html = markAstroOutput(renderPostList(posts, buildStats(posts)));
---
<Fragment set:html={html} />
